# Docker Compose

Start 1 service in detached mode
docker-compose up -d <service_name>

Stop 1 service
docker-compose stop <service_name>

Remove 1 service container
docker-compose rm <service_name>

Destroy all services
docker-compose down
